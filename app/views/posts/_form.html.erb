<%= form_for @post, :url => posts_path, id: 'post-form', remote: true, :html => { :multipart => true } do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.file_field :picture %>
  <%= f.submit %>
<% end %>
